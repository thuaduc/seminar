%% Agentic AI for Code Generation --- seminar talk (20 minutes).
%%
%% Companion deck to report/main.tex. Uses the same TUM template (tum/ is shared
%% via TEXINPUTS, see latexmkrc) and the same references.bib, so citations here
%% and in the report cannot drift apart.
%%
%%   latexmk  ->  build/main.pdf
%%
%% What to SAY lives in script.md, not in this file -- the deck is the visual, the
%% script is the delivery, and they are edited for different reasons.
%%
%% ---------------------------------------------------------------------------
%% THE SHAPE: ONE LINEAR STORY, NO ACTS.  (Restructured 2026-07-19.)
%%
%% This deck used to run in four acts on Wei's "asymmetry of verification", with a
%% turn at slide 12 and a benchmark-audit landing. All of that was deliberately CUT
%% -- the law slide, the "came in the box" table, the checker axis, the Olausson
%% experiment, the jagged edge, and the Feb/Jul-2026 timeline are all recoverable
%% from git history. What remains is a straight survey talk:
%%
%% RUNNING ORDER -- 11 numbered frames.
%%
%%   1        title
%%   2        what agentic AI for code generation is     (definition by contrast)
%%   3        how we got here                            (eras + the 2% -> 70% hook)
%%   4        the pipeline: the report's five-stage lens
%%   5-7      the three stages: plan (localisation), execute, refine
%%   8        SWE-bench: what a resolve rate is          (splits = a spoken aside)
%%   9        the numbers: the climb, told on the surveyed systems + Pro headroom
%%   10       [PROVISIONAL COOL POINT] no scaffold wins twice -- pending a
%%            dedicated brainstorm; may be replaced. Do not polish it yet.
%%   11       conclusion
%%
%% TWO SLIDES OF INTRODUCTION, BY DESIGN. Slide 2 says what the object of study IS
%% (by contrast with autocomplete and one-shot -- the two things everyone in the room
%% has used). Slide 3 says how it CAME ABOUT (eras), and carries the one hook number
%% pair: under 2 % (2023, Full) -> over 70 % (2026, Verified). The pair is on two
%% different rulers and the slide must keep saying so.
%%
%% CONCEPTS BEFORE MEASUREMENT. Slides 4-7 carry NO percentages -- deliberately.
%% SWE-bench is what a percentage in this talk means, and it is not defined until
%% slide 8, so a resolve rate before then is a number the audience cannot read.
%% The stage slides therefore make qualitative claims with directions ("ranking
%% functions helps"; "un-ranking lines beats perfect localisation") and leave the
%% magnitudes to the speaker. Slide 3's hook pair is the sole, flagged exception.
%% If you add a percentage to slides 4-7, you have broken this.
%%
%% THE NUMBERS SLIDE (9) USES ONLY SURVEYED SYSTEMS. The climb is told through the
%% systems the report actually covers -- RAG baseline, SWE-agent, AutoCodeRover,
%% Agentless, OpenHands (+critic) -- not through leaderboard entries the talk never
%% discusses. Splits differ per point, every point is tagged with its split, and the
%% footnote ("read the climb, not the deltas") is not optional.
%%
%% NO SECTION DIVIDERS, AND `sectionpage' IS OFF. Do not re-add \section here
%% without also re-adding `sectionpage' -- and do not re-add `sectionpage' without
%% meaning to spend a slide and ten seconds on it.
%%
%% NO HARD-CODED SLIDE NUMBERS IN BODY TEXT. The conclusion used to cite "the grid on
%% slide N" and the number went stale the first time the deck was reordered. Name the
%% slide, do not number it.
%% ---------------------------------------------------------------------------

\documentclass[
  aspectratio=169,
  font=times,      % match the report's body font
]{tumbeamer}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta, positioning, fit, decorations.pathreplacing}

% 'TUM more space' buys back the vertical room the default frametitle spends, and
% the headline strip is dropped on content pages: the TUM logo is already in the
% background, so a "Technical University of Munich" banner on every slide is a
% redundant use of the scarcest resource on a slide. The footline stays, for the
% page numbers.
\TUMbeamersetup{
  title page   = TUM tower,
  content page = TUM more space,
  footline     = TUM infoline,
  footline on  = {content page},
}

% The class installs the head/footline per frame style, so at \begin{document}
% beamer still sees the *empty* templates of the title page and computes a text
% height spanning the whole slide. Content then runs silently under the footline
% -- with no Overfull warning, because beamer does not know the footline is there.
% Install the content-page footline once up front and recompute, so \textheight
% actually excludes it and TeX warns when a frame is genuinely too full.
\makeatletter
\AtBeginDocument{%
  \setbeamertemplate{footline}[TUM infoline]%
  \beamer@calculateheadfoot
}
\makeatother

% LEGACY CHECKER PALETTE -- BACKUP SLIDES ONLY.
%
%   blue  -- the checker is real       (a test runner, a compiler, a human)
%   amber -- we made the checker up    (a trained critic, a generated test)
%   grey  -- there is no checker       (the model judging itself)
%
% This colour code belonged to the cut critique half of the talk (the "how real is
% the checker?" axis). The numbered slides no longer teach it, so it MUST NOT appear
% on any of them -- an untaught colour code reads as decoration at best and as a
% wrong hint at worst. It survives only on the backup refinement table, where the
% speaker can gloss it live in Q&A ("blue = real checker, amber = made-up, grey =
% none").
%
% Never use these in a block title: beamer sets block titles in white on TUM blue,
% so \oracle there is blue-on-blue. Body text and table cells only.
\definecolor{oracleblue}{HTML}{0065BD}   % TUM blue   -- the checker is real
\definecolor{proxyamber}{HTML}{E37222}   % TUM orange -- we made the checker up
\definecolor{selfgrey}{HTML}{6E6E6E}     % grey       -- there is no checker

\newcommand{\oracle}[1]{\textcolor{oracleblue}{\textbf{#1}}}
\newcommand{\proxy}[1]{\textcolor{proxyamber}{\textbf{#1}}}
\newcommand{\selfc}[1]{\textcolor{selfgrey}{\textbf{#1}}}

% Pipeline-figure edge colours. Deliberately OUTSIDE the axis palette above: the
% figure's two backward edges are not axis positions, and reusing oracle blue or
% proxy amber here would teach the audience a colour code the figure does not mean.
% Both are picked for projector contrast -- the previous red!60 / gray!55 washed out
% once the figure was scaled down.
\definecolor{feedbackred}{HTML}{C4071B}  % real feedback: execution -> refinement -> back
\definecolor{newtaskgrey}{HTML}{595959}  % the NON-feedback edge into intent capture

% Bar-chart colours for the Verified-vs-Pro comparison (backup slide only). ALSO
% outside the legacy palette: the two bars are two BENCHMARKS, not two checker
% positions. Two tones of one hue, because the chart's claim is "same weights,
% two conditions".
\definecolor{splitverified}{HTML}{8FAFC9}  % the saturated, widely-scraped split
\definecolor{splitpro}{HTML}{2E4A62}       % the fresher, harder, contamination-resistant one

\title{Agentic AI for Code Generation}
\subtitle{Seminar Report --- SS26}
\author{Thua Duc Nguyen}
\institute{Supervisor: Derui Zhu \\ \theDepartmentName, \theUniversityName}
\date{\today}


\begin{document}

\maketitle


% ============================================================================
% Introduction. Two slides: what the object of study IS, and how it CAME ABOUT.
% ============================================================================

% ---------------------------------------------------------------- slide 2 ---
% THE DEFINITIONAL SLIDE. It answers, in the audience's first thirty seconds, the
% question they actually have: what IS agentic AI for code generation? Nothing later
% in the deck has a subject until this lands, and an earlier draft skipped it -- it
% asserted a property of these systems ("the model runs the code") before saying what
% the systems were, which is a claim delivered to people who do not yet know what it
% is a claim ABOUT.
%
% The definition is given by CONTRAST, because that is the only way to make "agentic"
% mean something. Autocomplete and one-shot synthesis are the two things everyone in
% the room has already used, and in both of them the human closes the loop: you run
% the code, you read the error, you re-prompt. An agent closes its own. That is the
% entire content of the word, and drawing it as a progression means the audience
% derives the definition instead of being handed it.
%
% DO NOT DEFINE THE FIELD AS "SWE-bench". The trigger is wherever the developer
% already is -- an issue, a PR comment, a Slack thread, a ticket, a terminal -- and
% the output is a code change. Issue resolution is how the field is MEASURED, which
% is a different fact -- SPOKEN (script.md 2), not shown.
%
% ONE FIGURE, ONE LINE. The 2% -> 70% pair and the "measured on GitHub issues"
% aside both left this slide; the figure and the five-word takeaway are the slide.
\begin{frame}{What is agentic AI for code generation?}
  \small
  \begin{center}
  \resizebox{0.98\textwidth}{!}{%
  \begin{tikzpicture}[
    stagebox/.style={draw, rounded corners=3pt, align=center, font=\footnotesize,
                     minimum height=1.9cm},
    arr/.style={-{Stealth[length=2.4mm]}, very thick, black!55},
    brace/.style={decorate, decoration={brace, amplitude=5pt, mirror},
                  thick, black!60},
  ]
    \node[stagebox, fill=black!5,     draw=black!45, text width=3.5cm] (ac) at (0,0)
      {\textbf{Autocomplete}\\[2pt]
       {\scriptsize completes the line\\ you are typing}};
    \node[stagebox, fill=black!5,     draw=black!45, text width=3.9cm] (os) at (4.6,0)
      {\textbf{One-shot generation}\\[2pt]
       {\scriptsize a whole function,\\ from a docstring}};
    \node[stagebox, fill=TUMBlue!15, draw=TUMBlue, very thick, text width=5.6cm]
      (ag) at (10.4,0)
      {\textbf{An agent}\\[2pt]
       {\scriptsize plans $\cdot$ edits files $\cdot$ runs the tests\\
        $\cdot$ reads the traceback $\cdot$ tries again}};

    \draw[arr] (ac) -- (os);
    \draw[arr] (os) -- (ag);

    \draw[brace] (-1.85,-1.15) -- (6.65,-1.15);
    \draw[brace] (7.5,-1.15) -- (13.3,-1.15);

    \node[font=\scriptsize, align=center, text width=8.0cm, anchor=north]
      at (2.4,-1.65)
      {\textbf{The human closes the loop.}};
    \node[font=\scriptsize, align=center, text width=6.0cm, anchor=north]
      at (10.4,-1.65)
      {\textbf{The system closes its own loop.}};
  \end{tikzpicture}}
  \end{center}
  \vfill

  \begin{center}
    \textbf{Prose in; a patch out.}
  \end{center}
  \vfill
\end{frame}


% ---------------------------------------------------------------- slide 3 ---
% THE PROGRESSION SLIDE -- second half of the introduction. Slide 2 said what the
% object of study IS; this one says how it came about, as three eras on one time
% axis, ending in the hook: under 2 % (2023) -> over 70 % (2026).
%
% THE HOOK PAIR IS A TRAP, AND THE SLIDE SETS IT ON PURPOSE. Under 2 % is the
% original paper's *full* test set; over 70 % is *Verified*. Different rulers --
% which is why the italic caveat line under the pair is NOT optional, and why the
% ruler itself (SWE-bench, its splits) gets its own slide before any more numbers
% appear. Read honestly, the pair *is* the motivation: something enormous happened
% in three years. What happened is the talk.
%
% ERAS, NOT SYSTEMS. This timeline carries product-shaped milestones the audience
% already knows (Copilot, ChatGPT) plus the two hook numbers. The per-system numbers
% -- SWE-agent 18, Agentless 32, and so on -- belong to the numbers slide AFTER
% SWE-bench is defined. Do not pull them forward; the audience has no ruler yet.
\begin{frame}{Three years, three eras}
  \small
  \begin{center}
  \resizebox{0.97\textwidth}{!}{%
  \begin{tikzpicture}[
    era/.style={align=center, font=\footnotesize, anchor=south, text width=3.0cm},
    yr/.style={font=\scriptsize\bfseries, anchor=north, inner sep=3pt},
    dot/.style={circle, fill=TUMBlue, inner sep=1.8pt},
  ]
    \draw[-{Stealth[length=3mm]}, ultra thick, black!70] (-0.6,0) -- (14.2,0);

    \node[dot] (a) at (0.5,0) {};
    \node[yr] at (0.5,-0.12) {2021};
    \node[era] at (0.5,0.25)
      {\textbf{Autocomplete}\\{\scriptsize Copilot}};

    \node[dot] (b) at (3.8,0) {};
    \node[yr] at (3.8,-0.12) {2022};
    \node[era] at (3.8,0.25)
      {\textbf{One-shot generation}\\{\scriptsize ChatGPT}};

    \node[dot] (c) at (7.1,0) {};
    \node[yr] at (7.1,-0.12) {2023};
    \node[era] at (7.1,0.25)
      {\textbf{The task gets real}\\{\scriptsize SWE-bench}};

    \node[dot] (d) at (10.4,0) {};
    \node[yr] at (10.4,-0.12) {2024};
    \node[era] at (10.4,0.25)
      {\textbf{The agents arrive}\\{\scriptsize SWE-agent, Devin, OpenHands}};

    \node[dot] (e) at (13.3,0) {};
    \node[yr] at (13.3,-0.12) {2026};
    \node[era] at (13.3,0.25)
      {\textbf{Autonomy at scale}};
  \end{tikzpicture}}
  \end{center}
  \vfill

  \begin{center}
    {\usebeamerfont{framesubtitle}%
     under 2\,\%\quad\raisebox{0.1ex}{$\longrightarrow$}\quad
     \textcolor{TUMBlue}{over 70\,\%}}\\[0.2em]
    {\footnotesize of real GitHub issues resolved
     autonomously~\cite{jimenez2024swebench,swebench_leaderboard}}\\[0.35em]
    {\scriptsize\itshape (two different rulers --- defined in a few slides)}
  \end{center}
\end{frame}


% ---------------------------------------------------------------- slide 4 ---
% The report's lens, drawn: five stages every surveyed system fills in some form.
%
% THE CONTROL-SYSTEM GLOSS IS GONE, AND SO IS THE LAW GLOSS. The slide once closed
% on "intent is the uncontrolled input, generation is the plant..." (four pieces of
% undefined control-theory vocabulary) and later on "only one of them runs the code"
% (the cut critique's setup line). The closing line is now a plain statement of what
% the figure is FOR: a common vocabulary to compare systems in.
%
% The dotted edge ("new task, not feedback") is the figure's own honest detail --
% intent capture is the only box nothing returns to. The speaker may point at it;
% nothing later in this deck depends on it any more.
\begin{frame}{The agent pipeline}
  \begin{center}
  % 0.66 rather than 0.72: the closing line below shares the slide.
  \resizebox{0.66\textwidth}{!}{%
  \begin{tikzpicture}[
    stage/.style={draw, rounded corners=3pt, fill=TUMBlue!12, font=\small,
                  align=center, minimum height=0.9cm, minimum width=2.4cm},
    note/.style={font=\scriptsize, align=center, inner sep=1pt},
    arr/.style={-{Stealth[length=2.2mm]}, very thick},
    feedback/.style={-{Stealth[length=2.2mm]}, ultra thick, dashed,
                     feedbackred},
    newtask/.style={-{Stealth[length=2.0mm]}, very thick, dotted, newtaskgrey},
    fblab/.style={font=\scriptsize\bfseries, text=feedbackred, align=center,
                  inner sep=2pt, fill=white},
    ntlab/.style={font=\scriptsize, text=newtaskgrey, align=center, inner sep=2pt,
                  fill=white},
  ]
    \node[stage] (intent) at (0,0)      {Intent\\Capture};
    \node[stage] (plan)   at (3.6,0)    {Planning};
    \node[stage] (gen)    at (7.2,0)    {Code\\Generation};
    \node[stage] (exec)   at (7.2,-2.4) {Execution};
    \node[stage] (refine) at (3.6,-2.4) {Refinement};

    \draw[arr] (intent) -- (plan);
    \draw[arr] (plan) -- (gen);
    \draw[arr] (gen) -- (exec);
    \draw[arr] (exec) -- (refine);
    \draw[newtask] (refine.west) to[out=180, in=270]
      node[ntlab, pos=0.55] {new task,\\not feedback} (intent.south);

    \draw[feedback] (refine.north) -- node[fblab, right] {replan} (plan.south);
    \draw[feedback] (refine.north east) -- node[fblab, pos=0.5] {regenerate} (gen.south west);

    \node[note, above=0.2cm of intent] {NL issue or spec};
    \node[note, below=0.2cm of exec]   {test failures,\\traces};
  \end{tikzpicture}%
  }
  \end{center}
  \vfill
  \begin{center}
    \small
    Five stages --- \textbf{one vocabulary for every system in this talk.}
  \end{center}
  \vfill
\end{frame}


% ---------------------------------------------------------------- slide 5 ---
% Slides 5-7 walk the three stages the talk actually argues about, in pipeline order:
% plan -> execute -> refine. They come BEFORE SWE-bench on purpose: the stages are
% the vocabulary the later numbers are read in. Generation is deliberately not given
% its own slide -- what a generator can DO is a property of its action interface,
% and that detail lives in the backup material now.
%
% NO NUMBERS ON THIS SLIDE. Both results here used to carry a resolve-rate delta (SBFL
% +3 points; D4C beats perfect localisation by 10 %), and both are now stated as
% DIRECTIONS instead -- "ranking functions helps", "un-ranking lines beats perfect
% localisation". Nothing about the argument is lost, because the argument was never the
% magnitude: it is that localisation FLIPS SIGN at the function boundary. That is a
% qualitative claim and it survives intact. The magnitudes belong to the speaker (see
% script.md) and to the backup slide that holds them.
%
% The boundary is why the figure is worth its space. It carries the one result an
% audience cannot guess and that a sharp listener will otherwise raise as an objection
% ("but D4C says fault localisation is unnecessary"): localisation flips sign at the
% function boundary. Above it, it IS the task -- SBFL ranks functions and it helps.
% Below it, it fights the model's infilling prior -- D4C un-ranks lines and beats
% systems handed perfect localisation. Two results that look opposed, measuring opposite
% sides of one boundary. A full paragraph in the report; one glance here.
%
% Boundary rule is deliberately BLACK. It is not an axis position and not a feedback
% edge, so it must not borrow oracleblue, proxyamber or feedbackred -- that would teach
% a colour code the figure does not mean.
\begin{frame}{Planning is localisation, not design}
  \small
  \begin{center}
  \resizebox{0.92\textwidth}{!}{%
  \begin{tikzpicture}[
    lvl/.style={draw, rounded corners=2pt, fill=TUMBlue!12, font=\footnotesize,
                align=center, text width=1.8cm},
    arr/.style={-{Stealth[length=2mm]}, very thick},
    note/.style={font=\scriptsize, align=center, text width=7.2cm, anchor=north},
  ]
    \node[lvl, minimum height=2.0cm]  (repo)  at (0,0)   {Repository};
    \node[lvl, minimum height=1.3cm]  (files) at (2.7,0) {Files};
    \node[lvl, minimum height=0.75cm] (fns)   at (5.4,0) {Classes,\\functions};
    \node[lvl, minimum height=0.4cm]  (lines) at (9.2,0) {Lines};

    \draw[arr] (repo)  -- (files);
    \draw[arr] (files) -- (fns);
    \draw[arr] (fns)   -- (lines);

    \draw[ultra thick, black!80] (7.3,-1.15) -- (7.3,1.25);
    \node[font=\scriptsize\bfseries, align=center] at (7.3,1.72)
      {the function\\boundary};

    % The gloss that anchors the metaphor: the notes below say "above/below" (a
    % granularity hierarchy), but the funnel runs left/right on the page. These two
    % words are the bridge -- above the boundary = the coarser side.
    \node[font=\scriptsize\itshape, text=black!55] at (0,1.4)   {coarser};
    \node[font=\scriptsize\itshape, text=black!55] at (9.2,1.4) {finer};

    \node[note] at (2.9,-1.5)
      {\textbf{Above it, localisation \emph{is} the task} ---
       ranking functions \emph{helps}~\cite{zhang2024autocoderover}.};
    \node[note] at (11.4,-1.5)
      {\textbf{Below it, it hurts} --- \emph{no} line pointer beats
       \emph{perfect} localisation~\cite{xu2024d4c}.};
  \end{tikzpicture}}
  \end{center}
  \vfill
  \begin{center}
    Reasoning about \emph{where} the code is --- not \emph{how} to write it.
  \end{center}
\end{frame}


% ---------------------------------------------------------------- slide 6 ---
% Kept strictly DESCRIPTIVE: what runs, and what comes back.
%
% The figure's whole job is the vertical rule -- the model on one side, a real
% machine on the other. A boundary is the one thing prose is worst at and a diagram
% is best at; the audience sees the asymmetry without it being argued.
%
% The return arrow is feedbackred ON PURPOSE, and this is the one place a palette reuse is
% correct rather than sloppy: it is the SAME edge as the pipeline figure's execution ->
% refinement feedback arrow, zoomed in. Same colour, same meaning. The boundary rule stays
% black for the reason given on slide 5.
\begin{frame}{Execution: the code meets the environment}
  \small
  \begin{center}
  \resizebox{0.95\textwidth}{!}{%
  \begin{tikzpicture}[
    box/.style={draw, rounded corners=3pt, font=\footnotesize, align=center},
    arr/.style={-{Stealth[length=2.4mm]}, very thick},
    fb/.style={-{Stealth[length=2.4mm]}, very thick, feedbackred},
  ]
    \node[box, fill=TUMBlue!12, minimum width=3.4cm, minimum height=1.7cm] (agent)
      at (0,0) {\textbf{Agent}\\{\scriptsize a language model}};
    \node[box, fill=black!6, minimum width=5.0cm, minimum height=1.7cm] (env)
      at (10.2,0) {\textbf{a container}\\{\scriptsize the repo @ the parent commit}\\
                   {\scriptsize pytest $\cdot$ python $\cdot$ linter}};

    % The rule sits just outside the container, so both arrow labels stay clear of it: the
    % boundary is the point of the figure and nothing may be drawn on top of it.
    \draw[ultra thick, black!80] (6.6,-1.5) -- (6.6,1.5);

    \draw[arr] ([yshift=0.5cm]agent.east) --
      node[above, pos=0.28, font=\scriptsize] {a patch} ([yshift=0.5cm]env.west);
    \draw[fb] ([yshift=-0.5cm]env.west) --
      node[below, pos=0.72, font=\scriptsize, text=feedbackred, align=center]
      {failing tests, a traceback,\\a rejected edit} ([yshift=-0.5cm]agent.east);

    \node[font=\scriptsize, align=center]          at (0,2.1)
      {everything on this side,\\the model wrote};
    \node[font=\scriptsize\bfseries, align=center] at (10.2,2.1)
      {nothing on this side did};
  \end{tikzpicture}}
  \end{center}
  \vfill
  \begin{center}
    Not a score --- \emph{a symptom}, still to be read.
  \end{center}
\end{frame}


% ---------------------------------------------------------------- slide 7 ---
% Deliberately thin: name the stage, list what people actually do, move on. The
% mechanisms are real and published; the magnitudes live on the numbers slide
% (OpenHands + critic, 60.6 -> 66.4 on Verified) and in the backup refinement table.
% No figure here, deliberately -- planning and execution got one, and a third
% diagram in three slides would bury both.
\begin{frame}{Refinement: what to do with the signal}
  \small
  \vfill
  \begin{itemize}
    \item \textbf{Repair from the trace}~\cite{jiang2023selfevolve}
      \bigskip
    \item \textbf{Explain your own code}~\cite{chen2024selfdebug} --- \emph{only where
      tests exist}
      \bigskip
    \item \textbf{Sample $N$, rerank with a trained critic}~\cite{openhands2025verified}
  \end{itemize}
  \vfill
  \begin{center}
    Agents improve their own code.
  \end{center}
  \vfill
\end{frame}


% ---------------------------------------------------------------- slide 8 ---
% THE RULER. Every number in this talk is a resolve rate, and this slide is where
% that vocabulary is paid for -- immediately before the numbers slide that needs it.
%
% THE SPLITS TABLE WAS DEMOTED TO A LIST. The full four-row table (sizes, filters,
% caveats) cost half the slide and most of it was spoken anyway. What the audience
% must retain is one fact -- rates on different splits are not comparable -- and
% four names. The details (Lite leaks, Verified's screening, Pro's held-out repos)
% are the speaker's (script.md 8).
\begin{frame}{What SWE-bench measures}
  \small
  \begin{columns}[T]
    \begin{column}{0.53\textwidth}
      \textbf{One instance}~\cite{jimenez2024swebench} = a real merged PR:
      closed an issue $+$ touched the tests.
      \medskip

      \textbf{The agent sees} the repo before the merge $+$ the issue;
      \textbf{returns} a patch.
      \medskip

      \textbf{Graded by running the tests} --- the bug fixed, nothing broken.
      \medskip

      \textbf{Resolve rate} = both, on the first patch.\\
      \emph{Every number from here on is this one.}
    \end{column}
    \begin{column}{0.45\textwidth}
      \footnotesize
      \textbf{One benchmark, four splits:}
      \medskip

      \begin{itemize}
        \item \textbf{Full} --- 2{,}294, every scraped PR
        \item \textbf{Lite} --- 300, single-file patches
        \item \textbf{Verified} --- 500, human-screened
        \item \textbf{Pro} --- 1{,}865, held-out repos
      \end{itemize}
      \medskip

      \alert{Rates are not comparable across splits.} Verified is the most
      \emph{curated} split, not the hardest.
    \end{column}
  \end{columns}
\end{frame}


% ---------------------------------------------------------------- slide 9 ---
% THE NUMBERS, told on the systems the report actually surveys -- not on leaderboard
% entries the talk never discusses. One scatter over time; every point tagged with
% its split, because the points sit on THREE different rulers (Full / Lite /
% Verified) and a connecting line would assert a comparability that does not exist.
% Hence: marks only, no line, and the footnote caveat is not optional.
%
% THE PRO POINT IS HOLLOW AND SET APART on purpose: it is the same era as the ">70
% Verified" point, measured on the harder, held-out split, under a uniform harness
% (Scale's own leaderboard, mini-swe-agent, July 2026). It is the honest ceiling --
% the deck quotes MEASURED Pro numbers (~55-60), never the vendor self-reports
% (70-80), which mix harnesses and are verified by nobody.
%
% Dates are placement, not data: x positions are release months to one decimal of a
% year, good enough for a timeline read at 4 metres.
\begin{frame}{Three years, in numbers}
  \small
  \begin{center}
  \begin{tikzpicture}
    \begin{axis}[
      width=0.92\textwidth, height=5.6cm,
      xmin=2023.5, xmax=2027.1, ymin=0, ymax=97,
      xtick={2024,2025,2026},
      xticklabels={2024,2025,2026},
      ytick={0,20,40,60,80},
      ylabel={\% resolved (pass@1)},
      ylabel style={font=\scriptsize},
      tick label style={font=\scriptsize},
      axis lines=left,
      grid=major, grid style={dashed, gray!25},
      clip=false,
    ]
      \addplot[only marks, mark=*, mark size=2.2pt, color=TUMBlue] coordinates {
        (2023.8,2) (2024.25,18) (2024.45,19) (2024.6,32) (2024.85,26)
        (2025.5,66.4) (2026.4,74)};
      \addplot[only marks, mark=o, very thick, mark size=2.8pt, color=black!65]
        coordinates {(2026.6,60)};

      \node[font=\tiny, align=center, anchor=south] at (axis cs:2023.8,4)
        {RAG $+$ GPT-4\\$<$2 $\cdot$ Full};
      \node[font=\tiny, align=center, anchor=south east] at (axis cs:2024.2,20)
        {SWE-agent\\18.0 $\cdot$ Lite};
      \node[font=\tiny, align=center, anchor=north] at (axis cs:2024.45,16)
        {AutoCodeRover\\19.0 $\cdot$ Lite};
      \node[font=\tiny, align=center, anchor=south] at (axis cs:2024.6,34)
        {Agentless\\32.0 $\cdot$ Lite};
      \node[font=\tiny, align=center, anchor=west] at (axis cs:2024.95,26)
        {OpenHands\\26.0 $\cdot$ Lite};
      \node[font=\tiny, align=center, anchor=south] at (axis cs:2025.5,68.5)
        {OpenHands $+$ trained critic\\66.4 $\cdot$ Verified};
      \node[font=\tiny, align=center, anchor=south] at (axis cs:2026.4,76)
        {leaders\\$>$70 $\cdot$ Verified};
      \node[font=\tiny, align=center, anchor=north] at (axis cs:2026.6,57)
        {measured on \textbf{Pro}\\$\approx$55--60};
    \end{axis}
  \end{tikzpicture}
  \end{center}
  \vfill
  \begin{center}
    {\scriptsize Splits are \alert{not comparable} --- read the \emph{climb}, not
     the deltas.~\cite{jimenez2024swebench,yang2024sweagent,zhang2024autocoderover,%
     xia2024agentless,wang2024openhands,openhands2025verified,swebench_leaderboard,%
     scale2026pro}}
  \end{center}
\end{frame}


% --------------------------------------------------------------- slide 10 ---
% [PROVISIONAL COOL POINT -- pending a dedicated brainstorm; may be replaced.]
% The scaffold grid: every row holds the model and the benchmark fixed and varies
% only the scaffold -- the only structure that can attribute anything to the
% scaffold at all.
\begin{frame}{No scaffold wins twice}
  \small
  \begin{center}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    & \textbf{SWE-agent} & \textbf{OpenHands} & \textbf{mini-SWE-agent}
      & \textbf{spread} \\
    & {\scriptsize custom ACI} & {\scriptsize code-as-action}
      & {\scriptsize \raisebox{0pt}[0pt][0pt]{$\sim$}100 lines of Python}
      & \\
    \midrule
    GPT-5-mini        & 38.8 & 38.6 & \textbf{51.8} & $13.2$ \\
    Qwen3 Coder 480B  & 36.6 & \textbf{54.0} & 40.2 & $17.4$ \\
    DeepSeek R1       & \textbf{22.6} & 18.0 & 11.0 & $11.6$ \\
    \bottomrule
  \end{tabular}
  \end{center}
  {\footnotesize Resolve rate (pass@1) on SWE-bench Verified, April
   2026~\cite{suri2026codescout,miniswegent2026}.
   \textsc{aci} = agent--computer interface: the command set the model is handed.}
  \vfill
  \pause
  \begin{itemize}
    \item Same model, same benchmark: the scaffold alone is worth
      \textbf{11--17 points}.
    \item \alert{No scaffold wins twice} --- the best scaffold \emph{depends on the
      model}.
  \end{itemize}
  \vfill
\end{frame}


% ============================================================================
% The critique half of the deck (the turn, the checker axis, the Olausson
% experiment, the jagged edge, the benchmark-audit timeline) was cut in the
% 2026-07-19 restructure. Recoverable from git history.
% ============================================================================


% --------------------------------------------------------------- slide 11 ---
% [PROVISIONAL until the cool point is settled.] Three lines, no builds, no
% colour beyond the one blue number: the jump -> the loop -> the headroom. The
% middle line re-states the definitional slide's punchline on purpose: it is the
% one sentence the audience should leave with. The scaffold finding is SPOKEN
% (script.md 11), not shown -- it belongs to the cool-point slide, which may
% change.
\begin{frame}{Conclusion}
  \vfill
  \begin{itemize}
    \item In three years: under 2\,\% $\to$
      \textcolor{TUMBlue}{\textbf{over 70\,\%}}.
      \bigskip
    \item \textbf{What changed is the loop} --- the system runs its own code,
      and reads what comes back.
      \bigskip
    \item On the harder, held-out split: $\approx$60\,\%~\cite{scale2026pro}.
      \emph{The climb is not over.}
  \end{itemize}
  \vfill
\end{frame}


% ============================================================================
% Backup slides --- for Q&A, not counted in the 20 minutes.
% ============================================================================

\appendix

\begin{frame}[noframenumbering]{Backup: every refinement mechanism, sorted}
  \scriptsize
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}p{0.23\textwidth}p{0.07\textwidth}p{0.31\textwidth}p{0.29\textwidth}@{}}
    \toprule
    \textbf{Mechanism} & \textbf{Closed by} & \textbf{Signal}
      & \textbf{Characteristic failure} \\
    \midrule
    Self-Refine~\cite{madaan2023selfrefine}
      & \selfc{S} & Model critiques own output
      & Plateaus; nothing can contradict the generator \\
    Self-debug, no tests~\cite{chen2024selfdebug}
      & \selfc{S} & Self-generated explanation
      & Explains the bug as intended behaviour \\
    Self-debug, with tests~\cite{chen2024selfdebug}
      & \oracle{O}$+$\selfc{S} & Test verdict $+$ model diagnosis
      & Bounded by test coverage \\
    Reflexion~\cite{shinn2023reflexion}
      & \oracle{O} or \proxy{P} & Task reward where one exists; on HumanEval,
        self-generated tests
      & Filed as external, but its code result rests on a proxy \\
    SelfEvolve~\cite{jiang2023selfevolve}
      & \oracle{O} & Interpreter errors, traces
      & Silent (non-crashing) semantic bugs \\
    D4C~\cite{xu2024d4c}
      & \oracle{O} & Failing tests $+$ full function
      & Plausible-but-incorrect patches \\
    CRITIC~\cite{gou2024critic}
      & \oracle{O} & Self-critique routed \emph{through} an external tool
      & Needs a tool covering the error class \\
    Agentless validate~\cite{xia2024agentless}
      & \oracle{O}$+$\proxy{P} & Regression tests $+$ generated reproduction tests
      & The proxy half inherits the model's misreading \\
    Best-of-$N$ $+$ critic~\cite{openhands2025verified}
      & \oracle{O}$+$\proxy{P} & Executed rollouts ranked by a trained critic
      & Critic is a distilled proxy; cost scales with $N$ \\
    \bottomrule
  \end{tabular}
\end{frame}

% This figure is tall, not wide: constrain it by height, or it pushes the caption
% through the footline.
\begin{frame}[noframenumbering]{Backup: how plans are shaped}
  \centering
  \resizebox{!}{0.62\textheight}{%
  \begin{tikzpicture}[
    node distance=0.8cm and 1.2cm,
    thought/.style={draw, circle, fill=TUMBlue!10, minimum size=0.6cm,
                    font=\tiny},
    arr/.style={-{Stealth[length=1.2mm]}, semithick},
    backarr/.style={-{Stealth[length=1.2mm]}, semithick, dashed},
  ]
    \node[thought] (c1) {A};
    \node[thought, right=of c1] (c2) {B};
    \node[thought, right=of c2] (c3) {C};
    \node[thought, right=of c3] (c4) {D};
    \draw[arr] (c1) -- (c2);
    \draw[arr] (c2) -- (c3);
    \draw[arr] (c3) -- (c4);
    \node[above=0.15cm of c2, font=\scriptsize\bfseries] {Chain of Thought};

    \node[thought, below=2cm of c1] (t1) {A};
    \node[thought, below right=0.5cm and 0.4cm of t1] (t2a) {B$_1$};
    \node[thought, below left=0.5cm and 0.4cm of t1] (t2b) {B$_2$};
    \node[thought, below right=0.5cm and 0.2cm of t2a] (t3a) {C$_1$};
    \node[thought, below left=0.5cm and 0.2cm of t2a] (t3b) {C$_2$};
    \node[thought, below=0.7cm of t2b] (t3c) {C$_3$};
    \draw[arr] (t1) -- (t2a);
    \draw[arr] (t1) -- (t2b);
    \draw[arr] (t2a) -- (t3a);
    \draw[arr] (t2a) -- (t3b);
    \draw[arr] (t2b) -- (t3c);
    \draw[backarr, red!60] (t3b) to[bend right=45] (t2a);
    \node[above=0.15cm of t1, font=\scriptsize\bfseries] {Tree of Thoughts};
    \node[below=0.05cm of t3b, font=\tiny, text=red!70] {backtrack};

    \node[thought, below=2cm of c3] (g1) {A};
    \node[thought, below right=0.5cm and 0.4cm of g1] (g2a) {B$_1$};
    \node[thought, below left=0.5cm and 0.4cm of g1] (g2b) {B$_2$};
    \node[thought, below=1.2cm of g1] (g3) {C};
    \node[thought, below=0.7cm of g3] (g4) {D};
    \draw[arr] (g1) -- (g2a);
    \draw[arr] (g1) -- (g2b);
    \draw[arr, TUMBlue] (g2a) -- (g3);
    \draw[arr, TUMBlue] (g2b) -- (g3);
    \draw[arr] (g3) to[bend right=40]
      node[midway, right, font=\tiny, inner sep=1pt] {revisit} (g2a);
    \draw[arr] (g3) -- (g4);
    \node[above=0.15cm of g1, font=\scriptsize\bfseries] {Graph of Thoughts};
    \node[left=0.05cm of g3, font=\tiny, text=TUMBlue] {merge};
  \end{tikzpicture}%
  }
  \vfill
  {\footnotesize The evaluator, not the branching, makes the structure useful ---
   and it is what a repository-scale agent finds hardest to supply. \emph{What is
   a half-finished patch worth, before any test has run?}
   SWE-Search~\cite{antoniades2024swesearch} builds one (MCTS + LLM value
   function, $+23\,\%$ relative) --- and must invoke a model at every node.}
\end{frame}

% Holds the two numbers cut from the stage walk (slides 5 and 7), so they are one
% keystroke away if anyone asks for the magnitudes rather than the directions.
\begin{frame}[noframenumbering]{Backup: the numbers behind the stage walk}
  \small
  \begin{itemize}
    \item \textbf{Planning, above the function boundary}~\cite{zhang2024autocoderover}.
      AutoCodeRover adds spectrum-based fault localisation to rank \emph{functions}:
      \oracle{$+3$ points} on SWE-bench Lite.
      \medskip
    \item \textbf{Planning, below it}~\cite{xu2024d4c}. D4C hands the model the failing
      test and the \emph{whole function}, and gives it \emph{no line-level pointer at
      all} --- beating systems given perfect localisation by \textbf{10\,\%}. Below the
      function boundary, a pointer fights the model's infilling prior.
      \medskip
    \item \textbf{Refinement, with an explanation}~\cite{chen2024selfdebug}. Self-debug:
      up to \proxy{$+12\,\%$} --- but only where a test suite exists to run against.
      \medskip
    \item \textbf{Refinement, with a trained critic}~\cite{openhands2025verified}.
      OpenHands on Verified, best-of-$N$ reranked: 60.6 $\to$ \oracle{66.4\,\%}. (On the
      deck: the numbers timeline.)
  \end{itemize}
\end{frame}

% Benchmark detail the numbered deck no longer carries -- all of it real, all of it
% in the report, all of it Q&A ammunition. THE CHART NEEDS ITS CAVEATS SAID ALOUD:
% "read only the floor", "Pro is harder as well as fresher", "the column mixes
% harnesses". Use it only with time to caveat it properly.
\begin{frame}[noframenumbering]{Backup: the benchmark, in more detail}
  \small
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \centering
      \begin{tikzpicture}
        \begin{axis}[
          width=\textwidth, height=4.2cm,
          ybar, bar width=5pt, enlarge x limits=0.18,
          ymin=0, ymax=100, ylabel={\% Resolved},
          symbolic x coords={Opus 4.5, GPT-5.2, GPT-5.3, GLM-5.1},
          xtick=data,
          x tick label style={font=\scriptsize, rotate=18, anchor=east},
          yticklabel style={font=\scriptsize},
          ylabel style={font=\scriptsize},
          legend style={font=\tiny, at={(0.5,1.02)}, anchor=south,
                        legend columns=2, draw=none, fill=none,
                        /tikz/every even column/.append style={column sep=5pt}},
          grid=major, grid style={dashed, gray!30},
          nodes near coords, point meta=explicit symbolic,
          every node near coord/.append style={font=\tiny, rotate=90, anchor=west},
        ]
        % NOT the axis palette: these are two BENCHMARKS, not two axis positions.
        \addplot[fill=splitverified, draw=splitverified] coordinates {
          (Opus 4.5,80.9) [80.9] (GPT-5.2,80.0) [80.0]
          (GPT-5.3,85)   [85]   (GLM-5.1,78)   [78]};
        \addplot[fill=splitpro, draw=splitpro] coordinates {
          (Opus 4.5,45.9) [45.9] (GPT-5.2,56.4) [56.4]
          (GPT-5.3,56.8) [56.8] (GLM-5.1,58.4) [58.4]};
        \legend{Verified, Pro}
        \end{axis}
      \end{tikzpicture}
    \end{column}
    \begin{column}{0.46\textwidth}
      \footnotesize
      The \emph{same weights} drop $\approx$\,20--35 points on the contamination-resistant
      SWE-bench Pro~\cite{deng2025swebenchpro}.
      \medskip

      \alert{Read only the floor.} Pro is harder \emph{as well as} fresher, and the
      column mixes harnesses --- so the drop \emph{size} is not a measurement of
      memorisation.
      \medskip

      \textbf{91\,\%} of Verified is work a developer finishes \emph{within the
      hour}~\cite{ganhotra2025difficulty}. A system can gain points while solving no
      additional hard task.
      \medskip

      \textbf{July 2026: the ruler itself is under audit.} OpenAI flags
      $\sim$30\,\% of Pro's public tasks as broken and retracts its February
      recommendation to use it~\cite{openai2026pro}. Quote \emph{measured} Pro
      numbers, with that caveat attached.
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}[noframenumbering]{Backup: context and cost}
  \small
  \begin{itemize}
    \item \textbf{Longer windows do not dissolve the bottleneck.} Retaining full
      \emph{dependency} context works best; padding with loosely related code
      \emph{degrades} correctness~\cite{repoexec2025}.
      \medskip
    \item \textbf{Context explosion and semantic drift.} Appending every
      observation buries the critical early information~\cite{contextastool2025}.
      \medskip
    \item \textbf{Cost is quadratic in turns.} The whole prefix is re-sent each
      turn, so tokens grow as $O(n^2)$~\cite{turncontrol2025}. A property of the
      protocol, not an empirical finding; prompt caching cuts the constant, not
      the growth rate.
      \medskip
    \item \textbf{Resolve rate decouples from compute}~\cite{sweeffi2025}.
      Agentless leads on resolve rate (48\,\% with Qwen3-32B) at 83.1 API calls
      and 209.4\,s per issue. The frontier is accuracy \emph{per dollar}.
      \medskip
    \item \textbf{And verification is the human bottleneck.} In an RCT,
      experienced developers were 19\,\% \emph{slower} with early-2025 AI tools ---
      while believing they had been 20\,\% faster~\cite{metr2025rct}.
  \end{itemize}
\end{frame}

% The bibliography is already minimal: BibTeX prints only the entries actually cited.
% What made it sprawl over eight pages was beamer's default bibliography formatting,
% not the entry count:
%   - the default bibliography item is an ICON, which forces a wide hanging indent;
%   - beamer sets \itemsep between entries as if they were bullets.
% Text-numbered items, no icon, and zero itemsep pack them into ~3.
\setbeamertemplate{bibliography item}[text]
\setbeamerfont{bibliography item}{size=\tiny}
\setbeamerfont{bibliography entry author}{size=\tiny}
\setbeamerfont{bibliography entry title}{size=\tiny}
\setbeamerfont{bibliography entry location}{size=\tiny}
\setbeamerfont{bibliography entry note}{size=\tiny}

% \itemsep set in the frame body does NOT reach the bibliography: thebibliography
% opens its own list, which resets the spacing. Patch the environment itself.
\makeatletter
\let\beamer@oldthebibliography\thebibliography
\renewcommand{\thebibliography}[1]{%
  \beamer@oldthebibliography{#1}%
  \setlength{\itemsep}{0pt}%
  \setlength{\parsep}{0pt}%
  \setlength{\topsep}{0pt}%
}
\makeatother

\begin{frame}[noframenumbering, allowframebreaks]{References}
  \tiny
  \bibliographystyle{abbrv}
  \bibliography{references,slides-extra}
\end{frame}

\end{document}
